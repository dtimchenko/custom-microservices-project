server:
  port: ${PORT:8011}
  error:
    include-message: always
    include-binding-errors: always
spring:
  application:
    name: customer
  config:
    import: "optional:configserver:http://localhost:8010/"