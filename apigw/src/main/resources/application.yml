server:
  port: 8082
spring:
  application:
    name: api-gateway
  config:
    import: "optional:configserver:http://localhost:8010/"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka